@* @model ProductCatalog.Models.Product

@{
    ViewData["Title"] = "Создать продукт";
}

<h2>Создать продукт</h2>

<form asp-action="Create">
    <div class="form-group">
        <label asp-for="Name"></label>
        <input asp-for="Name" class="form-control" />
    </div>
    <div class="form-group">
        <label asp-for="Price"></label>
        <input asp-for="Price" class="form-control" />
    </div>
    <button type="submit" class="btn btn-primary">Сохранить</button>
</form>
<a asp-action="ProduktIndex">Назад к списку</a>
This is the Create view for adding a new product. It includes fields for the product name and price, and a submit button to save the product. *@

@model ProductCatalog.Models.Product

@{
    ViewData["Title"] = "Створити продукт";
}

<h2>Створити продукт</h2>

<form asp-action="Create" method="post">
    <div class="form-group">
        <label asp-for="Name">Назва товару</label>
        <input asp-for="Name" class="form-control" />
    </div>
    <div class="form-group">
        <label asp-for="Price">Вартість</label>
        <input asp-for="Price" class="form-control" />
    </div>
    <div class="form-group">
    <label asp-for="Category" class="control-label">Категорія</label>
    <input asp-for="Category" class="form-control" />
    <span asp-validation-for="Category" class="text-danger"></span>
    </div>
    <button type="submit" class="btn btn-success">Зберехти</button>
</form>
<a asp-action="ProduktIndex">Назад до списку</a>