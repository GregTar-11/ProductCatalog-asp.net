@model ProductCatalog.Models.Product

@{
    ViewData["Title"] = "Удалить продукт";
}

<h2>Удалить продукт</h2>

<div>
    <h4>Вы уверены, что хотите удалить этот продукт?</h4>
    <p>
        <strong>Название:</strong> @Model.Name <br />
        <strong>Цена:</strong> @Model.Price
    </p>
</div>

<form asp-action="DeleteConfirmed" method="post">
    <input type="hidden" asp-for="Id" />
    <button type="submit" class="btn btn-danger">Удалить</button>
    <a asp-action="ProduktIndex" class="btn btn-secondary">Отмена</a>
</form>
@* This is the Delete view for confirming the deletion of a product. It displays the product's name and price, and includes buttons to confirm or cancel the deletion. *@